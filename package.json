{
  "name": "pense-notes",
  "version": "1.0.0",
  "description": "Smart Notes and Reminders App",
  "main": "index.html",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "scripts": {
    "build": "mkdir -p www && cp index.html style.css app.js icon.png splash.png www/ || true",
    "sync": "npx cap sync"
  }
}